"use client";

import {ReactElement} from "react";

const UnitsLayout: React.FC<{children: React.ReactNode}> = ({
    children,
}): ReactElement => {
    return <>{children}</>;
};

export default UnitsLayout;
