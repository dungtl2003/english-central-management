"use client";

import {ReactElement} from "react";

const ClassesLayout: React.FC<{children: React.ReactNode}> = ({
    children,
}): ReactElement => {
    return <>{children}</>;
};

export default ClassesLayout;
