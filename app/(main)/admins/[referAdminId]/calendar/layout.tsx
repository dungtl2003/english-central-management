"use client";

import React, {ReactElement} from "react";

const CalendarLayout: React.FC<{children: React.ReactNode}> = ({
    children,
}): ReactElement => {
    return <>{children}</>;
};

export default CalendarLayout;
