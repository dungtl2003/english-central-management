"use client";

import React, {ReactElement} from "react";

const ClassDetailLayout: React.FC<{children: React.ReactNode}> = ({
    children,
}): ReactElement => {
    return <>{children}</>;
};

export default ClassDetailLayout;
