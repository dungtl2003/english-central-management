"use client";

import {ReactElement} from "react";

const UnitDetailLayout: React.FC<{children: React.ReactNode}> = ({
    children,
}): ReactElement => {
    return <>{children}</>;
};

export default UnitDetailLayout;
