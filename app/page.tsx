"use client";

import React, {ReactElement} from "react";
import ParallaxPage from "@/components/test/parallax";

const HomePage: React.FC = (): ReactElement => {
    return <ParallaxPage />;
};

export default HomePage;
